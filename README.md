# declmig-lib-core

ORM and migration source of truth for JSON Schema driven SQL migrations shared by web and API servers. Web servers may use the read-only feature; only this crate (and declarative-migrations) apply writes/migrations.
